/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.tixy.pk',"v2.ghostprotocols.pk",
        "ghostprotocols.pk",
        "daisyui.com",
    ],
      },
};

export default nextConfig;
