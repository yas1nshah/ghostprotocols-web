/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['images.ghostprotocols.pk',"v2.ghostprotocols.pk",
        "127.0.0.1",
        "ghostprotocols.pk",
        "daisyui.com",
    ],
      },
};

export default nextConfig;
